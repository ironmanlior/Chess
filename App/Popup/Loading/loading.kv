<Loading>:
    orientation: 'vertical'
    size_hint: None, None
    size: dp(50), dp(50)
    pos_hint: {"center_x": .5, "center_y": .5}
    MDSpinner:
        id: spinner
        size_hint: None, None
        size: dp(50), dp(50)
        pos_hint: {'center_x': .5, 'center_y': .5}
        # on__rotation_angle: root.check()